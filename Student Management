// Base Class (Encapsulation + Abstraction)
class Student {
    private String name;
    private int studentId;

    // Constructor
    public Student(String name, int studentId) {
        this.name = name;
        this.studentId = studentId;
    }

    // Getter methods (Encapsulation)
    public String getName() {
        return name;
    }

    public int getStudentId() {
        return studentId;
    }

    // Method (can be overridden)
    public void displayInfo() {
        System.out.println("Name: " + name + ", ID: " + studentId);
    }
}


// Inheritance
class Undergraduate extends Student {
    private String year;

    public Undergraduate(String name, int studentId, String year) {
        super(name, studentId); // call parent constructor
        this.year = year;
    }

    // Polymorphism (method overriding)
    @Override
    public void displayInfo() {
        System.out.println("Undergraduate - Name: " + getName() +
                ", ID: " + getStudentId() +
                ", Year: " + year);
    }
}


// Another child class
class Graduate extends Student {
    private String researchTopic;

    public Graduate(String name, int studentId, String researchTopic) {
        super(name, studentId);
        this.researchTopic = researchTopic;
    }

    // Polymorphism
    @Override
    public void displayInfo() {
        System.out.println("Graduate - Name: " + getName() +
                ", ID: " + getStudentId() +
                ", Research: " + researchTopic);
    }
}


// Manager class
class StudentManager {
    private Student[] students = new Student[10]; // array to store students
    private int count = 0;

    public void addStudent(Student student) {
        students[count] = student;
        count++;
    }

    public void showAllStudents() {
        for (int i = 0; i < count; i++) {
            students[i].displayInfo(); // Polymorphism happens here
        }
    }
}


// Main class
public class Main {
    public static void main(String[] args) {

        StudentManager manager = new StudentManager();

        Undergraduate s1 = new Undergraduate("Anom", 797, "2nd Year");
        Graduate s2 = new Graduate("Anom", 797, "Software");

        manager.addStudent(s1);
        manager.addStudent(s2);

        manager.showAllStudents();
    }
}

